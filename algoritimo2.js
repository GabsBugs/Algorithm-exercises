alert("Código funcionando!")
const ladoquadrado = prompt("Qual o tamanho do lado do quadrado?")


alert('O lado do quadrado é ' + ladoquadrado + '.')

const área = ladoquadrado * ladoquadrado
const perímetro = ladoquadrado * 4


alert('A área do quadrado é ' + área + ' e seu perímetro é ' + perímetro + '.' )